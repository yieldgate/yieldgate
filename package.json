{
  "name": "yieldgate",
  "version": "0.0.2",
  "license": "MIT",
  "private": true,
  "scripts": {
    "dev": "yarn hardhat:clean && yarn hardhat:compile && yarn frontend:dev",
    "frontend:dev": "yarn workspace frontend dev",
    "frontend:build": "yarn workspace frontend build",
    "frontend:start": "yarn workspace frontend start",
    "hardhat:clean": "yarn workspace hardhat clean",
    "hardhat:compile": "yarn workspace hardhat compile",
    "hardhat:chain": "yarn workspace hardhat chain",
    "hardhat:deploy": "yarn workspace hardhat deploy"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "**/hardhat",
      "**/hardhat/**"
    ]
  }
}